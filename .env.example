# Copy this file to .env.local and fill in your Supabase project values
# (Project Settings -> API in the Supabase dashboard).
# Without these two variables the site keeps working exactly as before,
# saving admin changes only in this browser's local storage.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
